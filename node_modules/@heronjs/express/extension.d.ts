import { IExtension, Maybe, Resolver, SecureContext, Type } from '@heronjs/common';
import { SecureProperty } from '@heronjs/common/secure';
import { Any } from '@heronjs/common/consts';
import { HttpRequest } from './types';
declare type ExtensionProps = {
    gateKeeper?: Type<SecureContext<HttpRequest | Any, SecureProperty>>;
    resolver?: Resolver<Any>;
    shareholder?: Any;
};
declare class Extension implements IExtension<ExtensionProps, Maybe<Any>> {
    #private;
    activate(args?: ExtensionProps): Promise<Maybe<Any>>;
    dispose(args?: ExtensionProps): Promise<void>;
}
export { Extension };
