import { Any, AuthResolver, SecureContext, Type } from '../../consts';
import { GuardProperty, SecureProperty } from '../../secure';
/**
 * Module secure context Declaration
 * @param context : @link(SecureContext)
 * @param resolver
 * @decorator
 */
export declare const GateKeeper: (context: Type<SecureContext<Any, SecureProperty>>, resolver: AuthResolver<Any>) => ClassDecorator;
/**
 * route secure context
 * @param guard
 * @link(SecureProperty)
 * @decorator
 */
export declare const Guard: (guard: GuardProperty) => any;
