/// <reference types="node" />
export declare enum StorageProtocol {
    S3 = 0,
    Azure = 1,
    GCS = 2,
    FTP = 3,
    LOCAL = 4
}
export declare type StorageCredential = {
    id: string;
    secret: string;
};
export declare type StorageConnection = {
    protocol: StorageProtocol;
    host: string;
    port?: string;
    credentials: StorageCredential;
};
export declare type StorageDefinition = {
    name: string;
    connection: StorageConnection;
    tls?: string | Buffer | boolean;
};
export declare const StorageClass: (clazz: StorageDefinition) => ClassDecorator;
