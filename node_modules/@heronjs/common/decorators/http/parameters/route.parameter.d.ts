import { ParameterContext } from '../../../consts';
export declare enum HttpParameter {
    REQUEST = "request",
    RESPONSE = "response",
    COOKIE = "cookie",
    QUERY = "query",
    PARAM = "param",
    HEADER = "header",
    ARRAY_QUERY = "array",
    IP = "ip",
    FUSE = "fuse",
    BODY = "body",
    ORIGINAL_URL = "originalUrl",
    URL = "url",
    AUTHORIZE = "authorize",
    QUERIES = "queries"
}
export declare type RouteParameter = Record<string | symbol, ParameterContext<HttpParameter>[]>;
