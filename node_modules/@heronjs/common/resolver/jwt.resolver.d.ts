import { Any, AuthResolver, HttpInComing, JwtOptions, JWTToken, WebSocketInComing } from '../consts';
export declare class JwtResolverStatic {
    private static $instance?;
    static create: (args: {
        options: JwtOptions;
    }) => Promise<JwtResolver | undefined>;
}
export declare class JwtResolver implements AuthResolver<JWTToken> {
    private readonly _jwt;
    private readonly options;
    http: [HttpInComing, string];
    ws: [WebSocketInComing, string];
    constructor(_jwt: Any, options: JwtOptions);
    resolve: (data?: string) => Promise<JWTToken>;
}
