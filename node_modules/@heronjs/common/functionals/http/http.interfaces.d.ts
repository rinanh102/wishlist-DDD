import { Any, OperationDispatcher, ParameterContext, RequestRoute, ViewDispatcher, ViewRoute } from '../../consts';
import { HttpParameter } from '../../decorators';
export interface IHttp<Req, Res, Next> extends OperationDispatcher<RequestRoute<Req, Res, Next>>, ViewDispatcher<ViewRoute<Req, Res, Next>> {
    parameterize: <Metadata extends ParameterContext<HttpParameter>>(args: any[]) => (meta: Metadata[]) => (req: Req, res: Res, next?: Next) => Any[];
}
