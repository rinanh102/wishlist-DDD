import { Downloadable, Streamable, Uploadable } from '../consts';
export interface IStorage extends Uploadable<string, void>, Streamable<string, void>, Downloadable<string, void> {
    connect: () => void;
}
