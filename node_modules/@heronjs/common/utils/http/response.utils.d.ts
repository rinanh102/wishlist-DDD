import { Paginate } from '../../consts';
import { RuntimeError } from '../../errors';
export declare class HttpResponseUtils {
    static output: <T>(data: T, pagination?: Paginate) => {
        data: T;
    };
    static error: <T extends RuntimeError>(err: T) => {
        error: string;
        message: string;
    };
}
