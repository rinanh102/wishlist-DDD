export interface FileWatcherEvent {
    path: string;
    ext: string;
    event: (file: string) => void;
}
export declare class Watcher {
    private watcher;
    private readonly _watcher;
    constructor(watcher: FileWatcherEvent[]);
    watch: (watch: FileWatcherEvent) => void;
}
