import { Optional } from '../../consts';
export declare type ClassError = {
    property: string;
    message: string[];
};
export declare type ClassErrorDetails = {
    context: string;
    error?: Optional<string>;
    errors: ClassError[];
};
export declare class ClassValidator {
    static validate<T extends object>(entity: T, context?: string): Promise<Optional<ClassErrorDetails>>;
}
