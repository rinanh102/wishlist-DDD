export declare class MomentDatetime {
    static DEFAULT_FORMAT: string;
    static to(date: Date, options: {
        toPattern: string;
        fromPattern?: string;
    }): string;
    static from(date: string, pattern: string): Date;
}
