import { TracingExporterType, TracingRemoteClient } from './enums.consts';
import { Any } from './type.consts';
export declare type TracingExporter = {
    type: TracingExporterType;
    client?: TracingRemoteClient;
};
/**
 * exportTimeoutMillis :30000
 * maxExportBatchSize : 512
 * maxQueueSize :2048
 * scheduledDelayMillis : 5000
 */
export declare type TracingGlobalConfiguration = {
    exportTimeoutMillis: number;
    maxExportBatchSize: number;
    maxQueueSize: number;
    scheduledDelayMillis: number;
};
/**
 * endpoint:
 *  - zipkin: @link(https://opentelemetry.io/docs/instrumentation/js/exporters/#zipkin)
 *  - jaeger: @link(https://opentelemetry.io/docs/instrumentation/js/exporters/#jaeger)
 *  - signoz: @link(https://signoz.io/opentelemetry/nodejs/)
 */
export interface TracingConfiguration {
    identity: string;
    exporter: TracingExporter;
    instrumentations?: [];
    endpoint?: {
        zipkin?: Any;
        jaeger?: Any;
        signoz?: Any;
    };
    global?: TracingGlobalConfiguration;
}
