import { Any, Type } from '../consts';
import { GenericProvider } from './provider.interface';
export interface ModuleMeta {
    id?: string;
    imports?: Array<Type<Record<Any, Any>>>;
    controllers?: Type<Record<Any, Any>>[];
    storages?: Type<Record<Any, Any>>;
    providers?: GenericProvider[];
    services?: GenericProvider[];
    websockets?: GenericProvider[];
    graphqls?: GenericProvider[];
    exports?: {
        controllers?: Type<Record<Any, Any>>[];
        storages?: Type<Record<Any, Any>>;
        providers?: GenericProvider[];
        services?: GenericProvider[];
        websockets?: GenericProvider[];
        graphqls?: GenericProvider[];
    };
}
