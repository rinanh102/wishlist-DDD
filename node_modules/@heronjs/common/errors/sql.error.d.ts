import { RuntimeError } from './runtime.error';
import { SQLErrors } from '../consts';
/**
 * SQL Query Error
 * @link(RuntimeError)
 * @link(Errors)
 * @link(SQLErrors)
 */
export declare class SQLError extends RuntimeError {
    constructor(code: SQLErrors, message: string);
}
