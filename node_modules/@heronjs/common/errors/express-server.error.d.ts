import { RuntimeError } from './runtime.error';
/**
 * Express Server Error
 * @link(RuntimeError)
 * @link(Errors)
 * @link(SYSErrors)
 */
export declare class ExpressServerError extends RuntimeError {
    constructor(message: string);
}
