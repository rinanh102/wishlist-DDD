import { RuntimeError } from './runtime.error';
import { EVNErrors } from '../consts';
/**
 * Express Server Error
 * @link(RuntimeError)
 * @link(Errors)
 * @link(EVNErrors)
 */
export declare class EventBusError extends RuntimeError {
    constructor(code: EVNErrors, message: string);
}
