import { RuntimeError } from './runtime.error';
/**
 * GraphSQL Server Error
 * @link(RuntimeError)
 * @link(Errors)
 * @link(SYSErrors)
 */
export declare class GraphQLError extends RuntimeError {
    constructor(message: string);
}
