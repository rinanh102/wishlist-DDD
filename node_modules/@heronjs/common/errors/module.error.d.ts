import { RuntimeError } from './runtime.error';
import { SYSErrors } from '../consts';
/**
 * Module Initialization Error
 * @link(RuntimeError)
 * @link(Errors)
 * @link(SYSErrors)
 */
export declare class ModuleError extends RuntimeError {
    constructor(code: SYSErrors, message: string);
}
