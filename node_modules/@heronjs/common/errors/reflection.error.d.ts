import { RuntimeError } from './runtime.error';
import { RTErrors } from '../consts';
/**
 * Reflect metadata error
 * @link(Errors)
 * @link(RTErrors)
 */
export declare class ReflectionError extends RuntimeError {
    constructor(code: RTErrors, message: string);
}
