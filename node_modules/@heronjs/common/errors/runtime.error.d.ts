/**
 * Common Runtime Error
 * @link(Error)
 */
import { Optional } from '../consts';
export declare class RuntimeError extends Error {
    readonly name: string;
    readonly code: number | string;
    readonly payload: Optional<Error>;
    private readonly _isFatal;
    constructor(name: string, code: number | string, message: string, payload?: Error, isFatal?: boolean);
    get isFatal(): Optional<boolean>;
}
