import { RuntimeError } from './runtime.error';
import { HttpStatusCodes } from '../consts';
/**
 * HttpError
 * @link(RuntimeError)
 * @link(Errors)
 * @link(HttpStatusCodes)
 */
export declare class APIError extends RuntimeError {
    constructor(code: HttpStatusCodes, message: string);
}
