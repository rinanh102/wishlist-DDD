import { RuntimeError } from './runtime.error';
import { ConstrainErrors } from '../consts';
export declare class ValidatorError extends RuntimeError {
    constructor(code: ConstrainErrors, message: string);
}
