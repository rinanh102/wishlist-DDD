import { ILogger } from '../functionals';
import { Any } from '../consts';
declare type LoggerProps = {
    level: 'warning' | 'error' | 'info' | 'verbose' | 'trace' | 'fatal';
};
export declare class Logger implements ILogger {
    private name;
    private readonly _logger;
    constructor(name: string, opts?: LoggerProps);
    error(message: string, meta?: Any): void;
    info(message: string, meta?: Any): void;
    warning(message: string, meta?: Any): void;
    verbose(message: string, meta?: Any): void;
    fatal(message: string, meta: Any | undefined): void;
    trace(message: string, meta: Any | undefined): void;
}
export {};
