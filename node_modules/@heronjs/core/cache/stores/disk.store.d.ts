import { CacheStore, DiskOptions, Optional } from '@heronjs/common';
import { CacheManager } from '@heronjs/common/consts';
export declare class DiskStore implements CacheStore {
    #private;
    private store;
    private constructor();
    static create: (config: DiskOptions) => Promise<DiskStore>;
    get: () => Optional<CacheManager>;
}
