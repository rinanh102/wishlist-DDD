import { CacheStore, MemoryOptions, Optional } from '@heronjs/common';
import { CacheManager } from '@heronjs/common/consts';
export declare class MemoryStore implements CacheStore {
    #private;
    private store;
    private constructor();
    static create: (config: MemoryOptions) => Promise<MemoryStore>;
    get: () => Optional<CacheManager>;
}
