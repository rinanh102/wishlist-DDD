import { CacheStore, Optional, RedisOptions } from '@heronjs/common';
import { CacheManager } from '@heronjs/common/consts';
export declare class RedisStore implements CacheStore {
    #private;
    private store;
    constructor(store: CacheManager);
    static create: (config: RedisOptions) => Promise<RedisStore>;
    get: () => Optional<CacheManager>;
}
