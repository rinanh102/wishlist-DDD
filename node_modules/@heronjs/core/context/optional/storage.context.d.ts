/// <reference types="node" />
import { HostOptions } from '@heronjs/common';
export interface StorageContext<T> {
    context: () => T;
}
export declare enum StorageClient {
    S3 = "s3",
    GCS = "gcs",
    AZURE = "azure",
    FTP = "ftp",
    SFTP = "sftp"
}
export declare type StorageConfig = HostOptions & {
    client: StorageClient;
    options: {
        key?: string;
        secret?: string;
        tls: {
            ca: string | Buffer;
        };
    };
};
