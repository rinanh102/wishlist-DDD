import { Listener } from '@heronjs/common/consts';
import { Context } from '../context';
import { ContextConfig } from '../../consts';
export interface ApplicationContext extends Context, Listener<ContextConfig, void> {
    modules: () => Set<ApplicationContext>;
    context: () => ApplicationContext;
}
