import { EventHandler, IEventContext } from '@heronjs/common';
export declare class EventContext implements IEventContext {
    #private;
    handler: <T>(name: string) => EventHandler<T>;
    register: <T>(name: string) => EventHandler<any>;
}
