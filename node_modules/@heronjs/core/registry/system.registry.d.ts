import { AbstractRegistry, RegistryType } from '@heronjs/common/registry';
import { Any, Instance } from '@heronjs/common';
export declare class SystemRegistry extends AbstractRegistry {
    #private;
    constructor();
    register(target: Any, name: string, type: unknown | string): Any;
    get(key: RegistryType): Map<Any, Instance>;
}
