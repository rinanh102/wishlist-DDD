import { Any, Optional } from '@heronjs/common';
import { DatabaseConfig, DatabaseConnector, DatabaseContext, ModuleDatabase } from '@heronjs/common/consts';
import { KnexClient } from '../../consts';
export declare abstract class AbstractKnexDatabaseTenancyClient<T extends KnexClient> implements ModuleDatabase<T>, DatabaseContext<T, DatabaseConnector<T>> {
    #private;
    config: Optional<Any>;
    protected constructor();
    context(): DatabaseContext<T, DatabaseConnector<T>>;
    database(databaseConfig?: DatabaseConfig | string): Optional<T>;
}
