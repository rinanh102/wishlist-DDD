import { Any, HeronJSConfig, IModuleDI } from '@heronjs/common';
declare type ActivateProps = {
    gateKeeper: Any;
    resolver?: Any;
    config?: HeronJSConfig;
    di?: IModuleDI;
    shareholder?: Any;
};
declare const activates: (modules: string[], props: ActivateProps) => Promise<Any[]>;
declare const activate: (module: string, props: ActivateProps) => Promise<Any>;
export { activate, activates };
