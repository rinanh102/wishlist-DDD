"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.activates = exports.activate = void 0;
const common_1 = require("@heronjs/common");
const errors_1 = require("@heronjs/common/errors");
const activates = async (modules, props) => {
    const results = new Array(modules.length);
    for (let i = 0; i < modules.length; i++) {
        const adt = activate(modules[i], props);
        adt && (results[i] = adt);
    }
    return results;
};
exports.activates = activates;
const activate = async (module, props) => {
    const pkg = await Promise.resolve().then(() => __importStar(require(module))).catch(_ => console.log(`skip loading extension ${module}!`));
    if (pkg) {
        const ext = new pkg.Extension();
        if (!ext || !ext['activate']) {
            throw new errors_1.ModuleError(common_1.SYSErrors.MODULE_ERR, `Failed to activate extension ${module}, activate and dispose methods are required!`);
        }
        return await ext['activate'].call(ext, props);
    }
};
exports.activate = activate;
